# Build / formal run artifacts (regenerated by make sim/syn/formal)
build/
formal/*/

# Cloned upstream sources (provenance in docs/xcheck-*.md; the assembled
# words are committed as sim/conf_pioexamples.svh so make sim never needs
# these — see sim/gen_conf_pioexamples.py for the regeneration recipe)
third_party/

# Python bytecode
__pycache__/
*.pyc
